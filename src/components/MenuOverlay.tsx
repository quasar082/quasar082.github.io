'use client';

interface MenuOverlayProps {
  isOpen: boolean;
  onClose: () => void;
}

export function MenuOverlay({isOpen, onClose}: MenuOverlayProps) {
  // Placeholder - full GSAP implementation in Task 2
  return null;
}
