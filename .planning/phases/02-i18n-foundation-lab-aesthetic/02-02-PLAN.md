---
phase: 02-i18n-foundation-lab-aesthetic
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/components/Header.tsx
  - src/components/LanguageSwitcher.tsx
  - src/components/MobileMenu.tsx
  - src/app/[lang]/page.tsx
  - src/app/[lang]/layout.tsx
  - src/messages/en.json
  - src/messages/vi.json
autonomous: false
requirements:
  - I18N-02
  - I18N-03

must_haves:
  truths:
    - "Header is visible at the top of the page with brand mark, nav links, and language switcher"
    - "Header background becomes blurred/tinted when user scrolls past 20px"
    - "Clicking EN or VI in the language switcher navigates to the other locale and updates all text"
    - "Language choice is saved to localStorage so returning visitors go straight to their preferred locale"
    - "On mobile, nav links collapse into a hamburger menu that opens a slide-in or dropdown"
    - "All visible UI text comes from translation files -- no hardcoded strings in component JSX"
    - "Home page shows translated content that changes when locale switches"
  artifacts:
    - path: "src/components/Header.tsx"
      provides: "Fixed header with scroll blur, brand mark, nav, language switcher"
      min_lines: 40
    - path: "src/components/LanguageSwitcher.tsx"
      provides: "EN | VI toggle that switches locale and persists to localStorage"
      min_lines: 15
    - path: "src/components/MobileMenu.tsx"
      provides: "Hamburger-triggered mobile navigation menu"
      min_lines: 25
    - path: "src/messages/en.json"
      provides: "Complete English translations for all UI strings"
    - path: "src/messages/vi.json"
      provides: "Complete Vietnamese translations for all UI strings"
  key_links:
    - from: "src/components/LanguageSwitcher.tsx"
      to: "localStorage"
      via: "localStorage.setItem('preferred-locale', newLocale)"
      pattern: "localStorage.*preferred-locale"
    - from: "src/components/LanguageSwitcher.tsx"
      to: "next-intl router"
      via: "useRouter from @/i18n/navigation"
      pattern: "router\\.replace.*locale"
    - from: "src/components/Header.tsx"
      to: "scroll state"
      via: "window scroll event listener"
      pattern: "addEventListener.*scroll"
    - from: "src/components/Header.tsx"
      to: "src/messages/*.json"
      via: "useTranslations('Header')"
      pattern: "useTranslations"
    - from: "src/app/[lang]/page.tsx"
      to: "src/messages/*.json"
      via: "useTranslations('Home')"
      pattern: "useTranslations"
---

<objective>
Build the Header with language switcher, mobile menu, and expand translations so all UI text is internationalized.

Purpose: This plan completes the user-facing i18n experience. After Plan 01 established the routing and token infrastructure, this plan adds the interactive UI that lets visitors switch languages, navigate the site, and see all text properly translated. It also serves as the visual proof that the design token system works (header styling, hover effects, accent colors).

Output: A fully styled header with scroll blur effect, EN/VI language toggle, mobile hamburger menu, and complete translation coverage for all current UI strings.
</objective>

<execution_context>
@/Users/mqcbook/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mqcbook/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-i18n-foundation-lab-aesthetic/02-CONTEXT.md
@.planning/phases/02-i18n-foundation-lab-aesthetic/02-RESEARCH.md
@.planning/phases/02-i18n-foundation-lab-aesthetic/02-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts from Plan 01 that this plan depends on. -->

From src/i18n/navigation.ts:
```typescript
export const {Link, redirect, usePathname, useRouter, getPathname} = createNavigation(routing);
```

From src/i18n/routing.ts:
```typescript
export const routing = defineRouting({
  locales: ['en', 'vi'],
  defaultLocale: 'en'
});
```

From src/lib/fonts.ts:
```typescript
export const marlinGeo: NextFont;  // variable: --font-marlin
export const saprona: NextFont;     // variable: --font-saprona
export const geistMono: NextFont;   // variable: --font-geist-mono
```

From src/app/globals.css (@theme inline tokens):
```css
--color-surface-base, --color-surface-elevated, --color-surface-overlay
--color-text-primary, --color-text-secondary, --color-text-muted, --color-text-accent
--color-accent, --color-accent-hover, --color-accent-muted
--color-border, --color-border-hover
--font-display, --font-body, --font-mono
```

Tailwind utilities available: bg-surface-base, bg-surface-elevated, bg-surface-overlay,
text-text-primary, text-text-secondary, text-text-muted, text-text-accent,
text-accent, border-border, border-border-hover, font-display, font-body, font-mono
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Header, LanguageSwitcher, and MobileMenu components</name>
  <files>
    src/components/Header.tsx
    src/components/LanguageSwitcher.tsx
    src/components/MobileMenu.tsx
  </files>
  <action>
    **LanguageSwitcher.tsx:**
    - `'use client'` directive
    - Import `useLocale` from `next-intl`
    - Import `usePathname`, `useRouter` from `@/i18n/navigation`
    - On locale switch: save to `localStorage.setItem('preferred-locale', newLocale)`, then call `router.replace(pathname, {locale: newLocale})`
    - Render two buttons "EN" and "VI" separated by a `|` span
    - Active locale button: `text-text-accent` color
    - Inactive locale button: `text-text-muted hover:text-text-secondary`
    - Transition duration: `duration-150` (per user decision: quick and snappy, 150-200ms)
    - Buttons should have `px-1` padding

    **Header.tsx:**
    - `'use client'` directive
    - Import `useTranslations` from `next-intl`, `useState`, `useEffect` from `react`
    - Import Lucide icons: use `Atom` (or `Beaker` or `FlaskConical`) for brand mark placeholder, `Menu` and `X` for mobile toggle
    - Import `LanguageSwitcher` and `MobileMenu`
    - Import `Link` from `@/i18n/navigation`

    Scroll effect:
    - `useState` for `scrolled` boolean
    - `useEffect` with passive scroll listener: set `scrolled = window.scrollY > 20`
    - When scrolled: `bg-surface-base/80 backdrop-blur-md border-b border-border`
    - When not scrolled: `bg-transparent`
    - Transition: `transition-colors duration-200`

    Layout:
    - `<header>` with `fixed top-0 left-0 right-0 z-50` (per baseline-ui: use fixed z-index scale, z-50 is standard for headers)
    - Inner container: `max-w-[1200px] mx-auto px-4 sm:px-6 flex items-center justify-between h-16`
    - Left: Brand mark with Lucide icon + "RayQuasar" text from `t('brand')`. Icon in `text-accent`, text in `text-text-primary font-display text-lg`. Wrapped in `Link href="/"`
    - Center/Right (desktop, `hidden md:flex`): Nav links (`t('home')`, `t('about')`, `t('projects')`, `t('blog')`). Use `Link` from `@/i18n/navigation`. Style: `text-text-secondary hover:text-text-accent transition-colors duration-150`. For now, all links point to `"/"` since sections don't exist yet -- they'll be hash links later.
    - Right: `LanguageSwitcher` component (hidden on mobile, visible md+)
    - Right (mobile, `md:hidden`): Hamburger button with `Menu` icon (or `X` when open). `aria-label="Toggle menu"` (per baseline-ui: icon-only buttons need aria-label)

    Mobile menu state:
    - `useState` for `mobileMenuOpen` boolean
    - Pass `isOpen` and `onClose` props to `MobileMenu`

    Hover effects: green accent glow on hover per user decision. On nav links, use `hover:text-text-accent` which gives the green accent color.

    **MobileMenu.tsx:**
    - `'use client'` directive
    - Import `useTranslations` from `next-intl`
    - Import `Link` from `@/i18n/navigation`
    - Import `LanguageSwitcher`
    - Import `motion`, `AnimatePresence` from `framer-motion`
    - Props: `isOpen: boolean`, `onClose: () => void`

    Render with `AnimatePresence`:
    - When `isOpen`, render a `motion.div` overlay/panel
    - Animation: slide down from top or fade in. Use `opacity` and `transform` only (per baseline-ui: only animate compositor props). Entry: `{ opacity: 0, y: -10 }` -> `{ opacity: 1, y: 0 }`. Exit: reverse.
    - Duration: `0.2` seconds (200ms max per baseline-ui)
    - Background: `bg-surface-elevated` with `border-b border-border`
    - Position: below header, full width
    - Content: nav links stacked vertically (`flex flex-col gap-4 p-6`), each link calls `onClose()` on click
    - Language switcher at the bottom
    - Use `text-text-secondary hover:text-text-accent` for links
    - Add `text-balance` on any heading text (baseline-ui rule)

    **IMPORTANT constraints from baseline-ui skill:**
    - Never animate layout properties (width, height, top, left, margin, padding)
    - Only animate compositor props (transform, opacity)
    - Ease-out on entrance
    - Never exceed 200ms for interaction feedback
    - Icon-only buttons must have aria-label
    - Never use h-screen, use h-dvh
    - Respect safe-area-inset for fixed elements (add `safe-area-inset` padding consideration)
  </action>
  <verify>
    <automated>cd /Users/mqcbook/Documents/github/my-source/rayquasar18.github.io && test -f src/components/Header.tsx && test -f src/components/LanguageSwitcher.tsx && test -f src/components/MobileMenu.tsx && grep -q "useTranslations" src/components/Header.tsx && grep -q "localStorage" src/components/LanguageSwitcher.tsx && npx next build</automated>
  </verify>
  <done>
    Header renders with brand mark (Lucide placeholder icon + "RayQuasar" text), navigation links, and EN/VI language switcher. Header gains backdrop blur when scrolled. Mobile hamburger opens a slide-down menu with nav links and language switcher. All text comes from translation files via useTranslations. Language switcher persists choice to localStorage.
  </done>
</task>

<task type="auto">
  <name>Task 2: Expand translations, integrate Header into layout, build home page content</name>
  <files>
    src/messages/en.json
    src/messages/vi.json
    src/app/[lang]/layout.tsx
    src/app/[lang]/page.tsx
  </files>
  <action>
    **Step 1: Update `src/app/[lang]/layout.tsx`**
    Add `<Header />` component import and render it inside the `<body>` tag, before `{children}`. Wrap the children in a `<main>` tag with appropriate padding-top to account for the fixed header (e.g., `pt-16`).

    **Step 2: Expand `src/messages/en.json`**
    Ensure all strings used in Header, LanguageSwitcher, MobileMenu, and Home page are present. Add any missing keys. Verify the structure covers:
    - `Metadata.title`, `Metadata.description`
    - `Header.brand`, `Header.home`, `Header.about`, `Header.projects`, `Header.blog`
    - `Hero.greeting`, `Hero.role`, `Hero.tagline`
    - `Home.welcome`, `Home.subtitle`, `Home.description` (add a longer description paragraph)
    - `LanguageSwitcher.en`, `LanguageSwitcher.vi`
    - `NotFound.title`, `NotFound.heading`, `NotFound.description`, `NotFound.homeLink`
    - `Footer.copyright` (placeholder for future)

    **Step 3: Expand `src/messages/vi.json`**
    Mirror the same structure as en.json with proper Vietnamese translations. USE PROPER DIACRITICS:
    - "Trang Chu" -> "Trang Chu" (with proper Vietnamese diacritics)
    - "Gioi Thieu" -> proper Vietnamese
    - "Du An" -> proper Vietnamese
    - "Ky su AI" -> proper Vietnamese
    - etc.

    If uncertain about exact Vietnamese diacritics, use the best approximation. The user can correct specific translations later -- the important thing is the structure is complete and no hardcoded strings remain.

    **Step 4: Update `src/app/[lang]/page.tsx`**
    Build a proper placeholder home page that demonstrates:
    1. The design token system is working (dark grid background, green accent colors, proper typography)
    2. Translations are working (all text from `useTranslations`)
    3. The layout and spacing work

    Content:
    - Hero-like section at the top (this is a PLACEHOLDER -- the real hero comes in Phase 5):
      - Large greeting text: `t('greeting')` with `font-display text-5xl md:text-6xl text-text-primary text-balance`
      - Role text: `t('role')` with `text-accent font-display text-2xl md:text-3xl`
      - Tagline: `t('tagline')` with `text-text-secondary text-lg md:text-xl`
    - A section below showing the aesthetic is working:
      - A card-like container with `bg-surface-elevated border border-border rounded-lg p-8`
      - Some body text using `text-text-secondary font-body`
      - A ghost button styled per user decision: transparent with green border, fill on hover. Classes: `border border-accent text-accent hover:bg-accent hover:text-surface-base transition-colors duration-150 px-6 py-2 rounded-md`
    - Generous vertical spacing between sections (use `spacing-section` or `py-32`)
    - Content max-width ~1200px centered: `max-w-[1200px] mx-auto px-4 sm:px-6`
    - Use `min-h-dvh` (NOT `min-h-screen` per baseline-ui)

    The page must be a client component (`'use client'`) because it uses `useTranslations`.

    **IMPORTANT: No hardcoded strings.** Every piece of text visible to the user must come from `useTranslations`. Verify by searching the JSX for any English or Vietnamese text that isn't wrapped in a `t()` call.
  </action>
  <verify>
    <automated>cd /Users/mqcbook/Documents/github/my-source/rayquasar18.github.io && npx next build && test -f out/en/index.html && test -f out/vi/index.html && grep -q "Header" src/app/\[lang\]/layout.tsx</automated>
  </verify>
  <done>
    Header is integrated into the locale layout and renders on all pages. Home page displays translated content demonstrating the full design system (dark grid background, green accents, custom fonts, token-based styling). All visible text comes from translation files -- no hardcoded strings in components. Both /en/ and /vi/ builds succeed.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual and functional verification of i18n + lab aesthetic</name>
  <action>
    Verify the complete i18n foundation and lab aesthetic visual system that was built in Tasks 1-2 and Plan 01. This checkpoint confirms the visual design, interactive behavior, and bilingual routing all work correctly before the phase is complete.

    What was built:
    1. Bilingual routing at /en/ and /vi/ with next-intl
    2. Dark lab aesthetic with green/emerald grid background and design token system
    3. Header with brand mark, navigation, EN/VI language switcher, scroll blur effect
    4. Mobile hamburger menu
    5. Root / page auto-redirects based on browser language
    6. Custom themed 404 page
    7. All UI text internationalized via translation files
  </action>
  <verify>
    1. Run `npm run dev` and visit http://localhost:3000
       - Should auto-redirect to /en/ or /vi/ based on your browser language
    2. At /en/:
       - Verify dark background with subtle green grid lines (graph-paper pattern)
       - Verify header has brand mark, nav links, and "EN | VI" switcher
       - Verify green accent colors on interactive elements
       - Verify custom fonts (Saprona for headings, MarlinGeoSQ for body)
    3. Click "VI" in the language switcher:
       - URL changes to /vi/
       - All text updates to Vietnamese
       - Language choice is remembered (refresh the page -- should stay on /vi/)
    4. Scroll down:
       - Header should gain a backdrop blur/dark tint effect
    5. Resize browser to mobile width (less than 768px):
       - Nav links collapse into hamburger icon
       - Tap hamburger to open mobile menu with nav links + language switcher
    6. Visit http://localhost:3000/en/nonexistent
       - Should show the custom 404 page with dark styling and links to both /en/ and /vi/
    7. Check Vietnamese text renders with proper diacritics (no missing/broken characters)
    8. Run `npm run build` -- should complete without errors
  </verify>
  <done>Human approves the visual design, language switching, mobile menu, and overall aesthetic. Any issues are noted for immediate fix before phase completion.</done>
</task>

</tasks>

<verification>
1. `npx next build` succeeds with no errors
2. `ls out/en/index.html out/vi/index.html out/404.html` -- all files exist
3. `grep 'hreflang' out/en/index.html` -- shows en and vi alternate links
4. All component files use `useTranslations` -- no hardcoded UI strings
5. `localStorage` persistence works for language preference
6. Header, LanguageSwitcher, MobileMenu components exist and are imported in layout
7. Visual verification: dark grid background, green accents, custom fonts, header blur on scroll
</verification>

<success_criteria>
- Language switcher toggles between /en/ and /vi/ and all text updates (I18N-02)
- All UI text comes from translation files, not hardcoded in JSX (I18N-03)
- Language preference persists in localStorage across page refreshes (I18N-04, reinforces)
- Header renders with scroll blur, brand mark, nav links, language switcher
- Mobile menu opens and closes with animated transition
- All design token utilities work (bg-surface-base, text-text-accent, etc.)
- Ghost button and hover effects match user-specified style
- Human approves the visual result
</success_criteria>

<output>
After completion, create `.planning/phases/02-i18n-foundation-lab-aesthetic/02-02-SUMMARY.md`
</output>
